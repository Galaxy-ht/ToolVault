# M1.2 Registry Interface Review Report

## Verdict

CONDITIONAL PASS

## Summary

当前 diff 只新增 `internal/registry/` 下 6 个文件，未修改 `api/`、`pkg/`、`cmd/` 或其他核心模块，也未引入第三方依赖。未发现 in-memory store、CRUD 存储实现、Runtime/Gateway/Policy/Credential/Protocol/Observability 越界。

之前的问题已基本修正：`VersionConflict` / `ExpectedVersion` 已移除，`Update` 改为 `ToolSpecUpdate`，状态变更通过 `SetStatus` 单独表达，错误分类也更清晰。

剩余条件项：`ToolSpecUpdate` 仍包含 `Actions` 和 `Metadata`，这超出 proposal 中 M1 最小接口草案的 update 字段范围，需要确认这是已批准的 `spec` 合同，或从 M1.2 interface 中移除。

## Blockers

No blockers found.

## Required Changes

- 确认或移除 [internal/registry/registry.go](/Users/egon/project/ToolVault/internal/registry/registry.go:87) 中 `ToolSpecUpdate.Actions` 和 `ToolSpecUpdate.Metadata`。Proposal 的 `ToolSpecUpdate` 草案只包含 `DisplayName`、`Description`、`Status`、`Tags`，且 M1 最小 `ToolSpec` 字段也未列出 `Actions` / `Metadata`。当前 interface 会把这两个字段固化为 M1.3 实现合同。

## Optional Improvements

- `Update` 与 `SetStatus` 的职责现在清楚，但 proposal 原文写的是 lifecycle transitions handled through `Update`。建议后续文档同步为 `SetStatus`，避免 Builder 读取 proposal 时产生歧义。
- `KindOf` 对常规 wrapping 已足够；`errors.Join` 场景暂未覆盖，M1 可不处理。

## Boundary Check

- 是否只聚焦 Registry interface？是。
- 是否修改 api/？否。
- 是否修改 pkg/？否。
- 是否越界到 in-memory store？否。
- 是否越界到 Runtime？否。
- 是否越界到 Gateway？否。
- 是否越界到 Policy？否。
- 是否越界到 Credential？否。
- 是否引入不必要依赖？否。

## Contract Review

Registry interface 基本最小可用，能支撑 M1.3 的 register/get/list/update/delete/status 实现。error contract 清晰、可测试、可通过 `errors.Is` 比较，并支持 wrapping。

status 语义合理：register 只允许 draft/active，transition matrix 覆盖 proposal 中的 M1 规则，未知 status 返回 `InvalidSpec` 也比之前更准确。version 回到 opaque string 语义，没有再引入并发控制。

M1.3 适配性良好，但 `Actions` / `Metadata` 是否属于 M1.2 Registry interface 需要收敛。

## Test Review

测试覆盖情况：

- error kind matching：已覆盖。
- error wrapping：已覆盖。
- default filters/options：已覆盖。
- status transition contract：已覆盖。
- compile-time interface constraints：已覆盖。

测试也新增了 update 不包含 identity/status 字段的约束。缺口是未明确测试 `ToolSpecUpdate` 只包含 proposal 批准字段。

## Verification

已运行并通过：

- `git status --short`：6 个新增文件，均在 `internal/registry/`。
- `git diff --stat`：6 files changed, 559 insertions.
- `make m1-registry-check`：通过。
- `make verify`：通过。

## Final Recommendation

建议条件合并：先处理或明确批准 `ToolSpecUpdate.Actions` / `Metadata` 的合同范围。除此之外，这版可以进入 M1.3 实现准备。
