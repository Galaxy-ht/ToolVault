# M1.6 Registry Acceptance Review Report

## Verdict

CONDITIONAL PASS

## Summary

当前分支是 `feat/m1-registry-acceptance`。实现和验收内容整体聚焦 M1 Registry acceptance / docs / examples / test polish，未发现 Gateway / Runtime / Policy / Credential / Protocol / Observability 越界，`make m1-registry-check` 和 `make verify` 均通过。

主要条件是当前 tracked diff 不完整：`README.md` 引用了 acceptance 和 example 文档，但这些文件仍是 untracked，按当前 Git diff 合并会产生断链。

## Blockers

- [README.md](/Users/egon/project/ToolVault/README.md:85) 引用了 `docs/examples/registry-basic.md` 和 `docs/acceptance/m1-registry.md`，但 `git diff --name-only` 只有 `README.md`，这些文件仍在 `git status` 中显示为 untracked。当前 diff 不能按原样合并。

## Required Changes

- 将以下 untracked 文件纳入变更集，或移除 README 对它们的引用：
  - [docs/acceptance/m1-registry.md](/Users/egon/project/ToolVault/docs/acceptance/m1-registry.md:1)
  - [docs/examples/registry-basic.md](/Users/egon/project/ToolVault/docs/examples/registry-basic.md:1)
  - [internal/registry/memory/example_test.go](/Users/egon/project/ToolVault/internal/registry/memory/example_test.go:1)
- M2 前需要人工明确接受或收窄 acceptance 文档列出的 proposal deviations：`Actions` / `Metadata`、`ExpectedVersion` / `VersionConflict`、`SetStatus`、duplicate `Name`。

## Optional Improvements

- acceptance checklist 可以把“当前 contract deviations 已被谁接受”补成明确审批记录。
- examples 已覆盖基本 register/get/list/status flow；可后续补一个 update/version conflict 示例，但不是 M1 合并前必须项。

## Boundary Check

- 是否只聚焦 M1 acceptance？是。
- 是否修改 api/？否；`api/` 不存在且无 diff。
- 是否修改 pkg/？否；`pkg/` 不存在且无 diff。
- 是否越界到 Runtime？否。
- 是否越界到 Gateway？否。
- 是否越界到 Policy？否。
- 是否越界到 Credential？否。
- 是否引入新功能？未在本 acceptance diff 引入新核心功能；但 acceptance 记录了承接自前序任务的 Registry contract 扩展。
- 是否引入不必要依赖？否；未发现第三方依赖或 Makefile/scripts 变更。

## Acceptance Review

- ToolSpec 是否完成：完成，但比 Proposal 001 更宽，包含 `Actions` / `Metadata`。
- Validation 是否完成：完成，覆盖 required fields、status、opaque version、actions、metadata。
- Registry interface 是否完成：完成，包含 register/get/list/update/delete/set-status。
- Error contract 是否完成：完成，包含 `AlreadyExists`、`NotFound`、`InvalidSpec`、`InvalidStateTransition`、`VersionConflict`。
- In-memory implementation 是否完成：完成，标准库实现，带锁和 defensive copy。
- Lifecycle 是否完成：完成，hard delete、status transition、ExpectedVersion update 语义清楚。
- Tests 是否充分：充分覆盖 M1 主要路径；并发覆盖基础 register/get/list。
- Docs / examples 是否足够进入下一阶段：内容足够，但必须先纳入 Git 变更集。

## M1 Completion Decision

- M1 是否完成？条件完成。
- 是否建议 merge m1/registry → main？不建议按当前 diff 直接合并；完成 Required Changes 后建议合并。
- 是否建议进入 M2？建议在合并后进入 M2 planning。
- 进入 M2 前是否还有必须修复的问题？需要人工确认或收窄 documented deviations，避免 Gateway 把未批准扩展当稳定 contract 使用。

## Verification

已运行并通过：

- `make m1-registry-check`
- `make verify`

确认命令输出：

- `git status --short`：`README.md` modified；`docs/acceptance/`、`docs/examples/`、`internal/registry/memory/example_test.go` untracked。
- `git diff --stat`：`README.md | 28 ++++++++++++++++++++++++++--`
- `make m1-registry-check`：passed。
- `make verify`：passed。

## Final Recommendation

建议合并前先把 untracked acceptance/docs/example test 纳入变更集，并由人确认 Registry contract deviations。完成后，建议合并进入 `main` 并开始 M2 planning。
