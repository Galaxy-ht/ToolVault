# M1.1 ToolSpec Review Report

## Verdict

CONDITIONAL PASS

## Summary

当前 Git diff 只新增：

- `/Users/egon/project/ToolVault/internal/registry/spec/spec.go`
- `/Users/egon/project/ToolVault/internal/registry/spec/spec_test.go`

验证命令通过：

- `git status --short`：仅 `internal/registry/spec/` 下 2 个新增文件
- `git diff --stat`：2 files changed, 399 insertions
- `make m1-registry-check`：通过
- `make verify`：通过

实现没有引入 Registry CRUD、store、runtime、gateway、policy、credential、protocol、observability，也没有修改 `api/` 或 `pkg/`。

条件点：当前实现加入了 `Actions` 和 `Metadata`，而已批准的 Proposal 001 中最小 `ToolSpec` 只列出 `ID/Name/DisplayName/Description/Version/Status/Tags`。如果本次 M1.1 ToolSpec Model + Validation 已经获得人类批准扩展到 actions/metadata，则可以视为通过；否则需要先更新任务合同或 proposal，避免实现超出已批准模型。

## Blockers

No hard blockers found.

## Required Changes

- 需要补齐治理一致性：确认或记录 `Actions`、`Metadata` 已被批准纳入 M1.1 ToolSpec 最小模型；否则应移除这两个字段及相关校验，回到 Proposal 001 的最小字段集。

## Optional Improvements

- 可补充测试 `ToolID` 仅空白字符的情况。
- 可补充 action invalid name 的专门测试。
- 可补充 version control-character 测试，目前只覆盖 whitespace。
- Metadata 当前只校验 key，不校验 value；如果 value 规则有意保持宽松，建议在测试名或注释中明确。

## Boundary Check

- 是否只聚焦 ToolSpec？是，只有 model + validation。
- 是否修改 api/？否。
- 是否修改 pkg/？否。
- 是否越界到 Registry CRUD？否。
- 是否越界到 Runtime？否。
- 是否越界到 Gateway？否。
- 是否越界到 Policy？否。
- 是否越界到 Credential？否。
- 是否引入不必要依赖？否，仅标准库。

## Test Review

测试覆盖情况：

- valid spec：已覆盖。
- missing required fields：已覆盖 top-level required fields。
- invalid name：已覆盖。
- invalid version：已覆盖 whitespace case。
- invalid status：已覆盖。
- empty actions：已覆盖。
- duplicate actions：已覆盖。
- action missing required fields：已覆盖 name 和 description。
- metadata edge cases：已覆盖 nil、empty、max entries、too many entries、blank key、surrounding whitespace、overlong key。

总体测试覆盖满足本次检查清单，边界路径较完整。

## Verification

已运行：

```sh
git status --short
git diff --stat
make m1-registry-check
make verify
```

结果：`make m1-registry-check` 和 `make verify` 均通过。

## Final Recommendation

建议标记为 CONDITIONAL PASS。代码边界和验证结果良好，但需要先把 `Actions` / `Metadata` 纳入已批准 M1.1 ToolSpec 合同，或收窄实现以匹配 Proposal 001 的最小模型。
