# M1.4 Registry Lifecycle Review Report

## Verdict

CONDITIONAL PASS

## Summary

当前 diff 只修改 `internal/registry/` 下 5 个文件，未修改 `api/`、`pkg/` 或其他核心模块，未引入第三方依赖，`make m1-registry-check` 和 `make verify` 均通过。

主要 lifecycle 语义清晰：`Update` 需要 `ExpectedVersion`，版本不匹配返回 `VersionConflict`，成功 update 必须 bump `ToolSpec.Version`；`SetStatus` 只处理状态转换并保留 version；`Delete` 仍是 hard delete；`Get/List` 对 disabled/deprecated/delete 后行为有测试覆盖。

## Blockers

No blockers found.

## Required Changes

- [internal/registry/memory/registry.go](/Users/egon/project/ToolVault/internal/registry/memory/registry.go:51) 新增 duplicate `Name` 拒绝逻辑，不属于 version / status lifecycle，且 Proposal 001 明确写的是 duplicate tool ID。建议移出本 lifecycle diff，或补充已批准的 Registry identity 规则说明。

## Optional Improvements

- [internal/registry/registry.go](/Users/egon/project/ToolVault/internal/registry/registry.go:104) `DefaultUpdateOptions()` 返回零值，但 `Update` 现在要求 `ExpectedVersion` 非空。行为可接受，但建议后续文档说明默认 options 不代表有效 update options。
- 历史 review 中 `Actions` / `Metadata` 的批准状态仍是残余治理风险；本次 diff 未新增该范围，但后续合并前仍应确认。

## Boundary Check

- 是否只聚焦 Registry lifecycle？基本是；但 duplicate `Name` 检查偏离 lifecycle。
- 是否修改 api/？否。
- 是否修改 pkg/？否。
- 是否越界到 Runtime？否。
- 是否越界到 Gateway？否。
- 是否越界到 Policy？否。
- 是否越界到 Credential？否。
- 是否引入不必要依赖？否。

## Lifecycle Review

- version conflict 规则是否合理：合理。`ExpectedVersion` 必填， mismatch 返回 `VersionConflict`。
- version bump 规则是否合理：合理。成功 metadata update 必须提供不同的新 opaque version。
- status transition 是否合理：合理，沿用已批准状态矩阵，`SetStatus` 保留 version。
- delete / unregister 语义是否合理：合理，仍是 hard delete。
- get / list 对状态的行为是否清晰：清晰，默认包含 disabled/deprecated，delete 后不可见。
- 是否适合后续 Runtime / Gateway 使用：适合，未引入 Runtime/Gateway 依赖；但 version conflict 合同属于 Registry interface 扩展，应确认 M1.4 lifecycle 已批准。

## Test Review

- duplicate identity：覆盖 ID，也新增 Name duplicate；Name 规则需批准。
- update success：覆盖。
- version conflict：覆盖。
- immutable field update：覆盖 ID/Name/Status 不变。
- valid status transitions：覆盖。
- invalid status transitions：覆盖。
- delete / unregister：覆盖 hard delete。
- get / list after delete：覆盖。
- disabled / deprecated behavior：覆盖。
- lifecycle error matching：覆盖 `VersionConflict` 等 error kind matching。

## Verification

已运行并通过：

- `git status --short`：5 个 modified 文件，均在 `internal/registry/`。
- `git diff --stat`：5 files changed, 202 insertions(+), 19 deletions(-)。
- `make m1-registry-check`：通过。
- `make verify`：通过。

## Final Recommendation

建议条件合并：先处理或批准 duplicate `Name` identity 规则。除此之外，本次 lifecycle/version/status 变更边界清楚、测试充分，可以进入合并准备。
